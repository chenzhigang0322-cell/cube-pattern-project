# 蓝牙连接问题修复说明

## 问题描述
用户报告手机和电脑可以找到魔方的蓝牙,但点击连接后没有扫描到可用设备。

## 问题分析

1. **Web Bluetooth API的限制**
   - Web Bluetooth API需要特定的蓝牙服务和特征值才能正确扫描设备
   - 原代码使用默认参数调用`connectGanCube()`,没有指定任何过滤条件
   - 这可能导致扫描不到设备或扫描范围过大

2. **调试信息不足**
   - 原代码缺少详细的调试日志
   - 难以定位连接失败的具体原因

## 解决方案

### 1. 创建新的Hook文件
创建了`use-cube-connection-new.ts`文件,包含以下改进:

- **添加详细的蓝牙服务UUID**:
  ```typescript
  optionalServices: [
    '00001800-0000-1000-8000-00805f9b34fb', // Generic Access
    '00001801-0000-1000-8000-00805f9b34fb', // Generic Attribute
    '0000ffe0-0000-1000-8000-00805f9b34fb', // GAN魔方服务
    '0000ffe1-0000-1000-8000-00805f9b34fb', // GAN魔方特征值
  ]
  ```

- **添加调试日志**:
  - 连接开始时输出提示信息
  - 扫描设备时输出状态信息
  - 连接成功后输出设备名称
  - 接收魔方事件时输出事件详情

- **改进错误处理**:
  - 提供更详细的错误信息
  - 区分不同类型的连接错误
  - 给出具体的解决建议

### 2. 更新TopBanner组件
将TopBanner组件中的导入语句更新为使用新的hook:
```typescript
import { useCubeConnection } from '@/hooks/use-cube-connection-new'
```

## 使用说明

### 连接魔方前的准备

1. **确保魔方已开机**
   - 按下魔方上的电源按钮
   - 等待指示灯亮起

2. **进入配对模式**
   - 长按魔方上的配对按钮(通常位于电源按钮附近)
   - 指示灯会快速闪烁,表示进入配对模式
   - 如果魔方已与其他设备连接,需要先断开

3. **确保浏览器支持**
   - 使用Chrome或Edge浏览器
   - 确保在localhost或HTTPS环境下访问

4. **授予蓝牙权限**
   - 首次连接时,浏览器会请求蓝牙权限
   - 点击"允许"授予访问权限

### 连接步骤

1. 点击页面上的"连接魔方"按钮
2. 浏览器会弹出蓝牙设备选择对话框
3. 在对话框中选择您的GAN 356 i3魔方
4. 等待连接建立

### 调试信息

打开浏览器控制台(F12),可以看到详细的连接日志:
- "开始连接魔方..." - 连接过程开始
- "正在扫描蓝牙设备..." - 正在扫描设备
- "提示: 请确保魔方已开机并处于配对模式" - 操作提示
- "成功连接到魔方: [设备名称]" - 连接成功
- "收到魔方事件: [事件类型] [事件详情]" - 接收魔方事件

### 常见问题

**Q: 点击连接后没有弹出设备选择对话框**
A: 可能原因:
- 浏览器不支持Web Bluetooth API,请使用Chrome或Edge
- 浏览器未授予蓝牙权限,请在浏览器设置中检查
- 不是在localhost或HTTPS环境下访问

**Q: 弹出对话框但找不到魔方**
A: 可能原因:
- 魔方未开机
- 魔方未进入配对模式
- 魔方已被其他设备连接
- 魔方距离太远或信号太弱

**Q: 连接后立即断开**
A: 可能原因:
- 魔方电量不足
- 魔方被其他设备强制断开
- 浏览器蓝牙权限被撤销

## 技术细节

### 蓝牙服务UUID

- **Generic Access Service (0x1800)**: 蓝牙标准服务,提供设备名称等信息
- **Generic Attribute Service (0x1801)**: 蓝牙标准服务,用于属性协议
- **GAN魔方服务 (0xFFE0)**: GAN魔方专用的蓝牙服务
- **GAN魔方特征值 (0xFFE1)**: 用于与魔方通信的特征值

### Web Bluetooth API限制

- 只能在HTTPS或localhost环境下使用
- 需要用户手势触发(如点击事件)
- 浏览器必须支持Web Bluetooth API
- 需要用户明确授权蓝牙权限

## 下一步

如果问题仍然存在,可以尝试:

1. 检查浏览器控制台的错误信息
2. 尝试使用不同的浏览器
3. 重启魔方和电脑/手机的蓝牙
4. 更新浏览器到最新版本
5. 联系魔方卖家确认固件版本

## 文件变更

- 新建: `src/hooks/use-cube-connection-new.ts` - 第一次尝试的版本(已废弃)
- 新建: `src/hooks/use-cube-connection-v2.ts` - 当前使用的版本
- 修改: `src/components/patterns/TopBanner.tsx` - 更新导入语句
- 新建: `BLUETOOTH_FIX.md` - 本说明文档

## 版本更新说明

### v2版本改进

根据实际测试发现,`gan-web-bluetooth`库的`connectGanCube`函数不接受参数,因此v2版本:

1. 移除了所有传递给`connectGanCube`的参数
2. 保留了详细的调试日志
3. 保留了改进的错误处理

这样既保持了调试能力,又避免了参数不兼容的问题。
